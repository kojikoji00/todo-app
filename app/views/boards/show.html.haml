%h1 Brankets
= link_to board_path(@board) do
  - @tasks.each do |task|
    = render 'commons/task', task: task

.container
  = link_to new_board_task_path(@board) do
    .add_card
      %h2 + Add new card